name: Staging

on:
  workflow_call:
    inputs:
      DISPLAY_VERSION:
        description: Display Version
        required: true
        type: string
    outputs:
        versiondisplay:
          description: "Display Version"
          value: ${{ inputs.DISPLAY_VERSION }}

jobs:
        stage:
          name: Stage Job
          runs-on: ubuntu-22.04
          steps:            
            - name: Test input
              run: |
                echo ${{ inputs.DISPLAY_VERSION }}
